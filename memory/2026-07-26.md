# 2026-07-26

## Heartbeat: Fixed Cron Job Model Failures & Manual Sync

The heartbeat executor fired a "Sync ALERT: last sync was 65h ago" — last successful sync was Jul 23.

### Root Cause
The Daily Workspace Sync (and 4 other cron jobs) had been failing for ~4 consecutive runs because their assigned model (`flash` → `google/gemma-4-31b-it`) was returning 429, and all fallback models were also failing:
- `deepseek/deepseek-reasoner` → 400 (model name rejected — DeepSeek API now requires `deepseek-v4-pro` or `deepseek-v4-flash`)
- `openrouter/openrouter/auto` → 402 (credits exhausted)
- `openai/gpt-5.4` → rate-limited (Codex subscription limit)

### Actions Taken
1. **Manual sync**: Ran `daily-sync.sh` at 10:36 UTC — success. Pushed 4 new nightly reports and updated log analysis that had accumulated since Jul 23.
2. **Fixed 5 cron jobs** — updated `payload.model` from `flash` (dead) to `heartbeat-free` (nemotron-3-nano-30b-a3b:free) with `deepseek/deepseek-chat` as fallback:
   - Daily Workspace Sync (10am PT)
   - Daily Sync Watchdog (11am PT)
   - Daily Journal Synthesis (07:10 UTC)
   - Daily Log Health Check (07:20 UTC)
   - Daily qmd Index Refresh (08:00 UTC)
3. **Nightly Activity Report** was not failing — it's already using `heartbeat-free` and last ran successfully.

### Note
`deepseek/deepseek-reasoner` alias is broken with DeepSeek's current API. The correct model ID is now `deepseek-v4-pro` or `deepseek-v4-flash`. This needs updating in the gateway config if reasoning models are wanted for main session.

### Files Created by Sync
- `memory/logs/analysis-2026-07-24.md`
- `memory/logs/nightly-report-2026-07-23.md`
- `memory/logs/nightly-report-2026-07-24.md`
- `memory/logs/nightly-report-2026-07-25.md`
