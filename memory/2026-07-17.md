# 2026-07-17

## 11:04 UTC - Heartbeat executor hardening

- Heartbeat wake followed `HEARTBEAT.md` auto-improvement flow instead of only replying.
- Ran `node /home/claw/.openclaw/workspace/scripts/heartbeat-executor.js`; the pre-fix result was routine healthy output for `qmd`.
- Spawned two parallel explorer subagents to review recent heartbeat mistakes and inspect executor robustness.
- Confirmed recent local issues:
  - prior heartbeat alert loops were already documented in `memory/2026-07-12.md`
  - `cron`, `qmd`, and `health` checks in `scripts/heartbeat-executor.js` were using placeholder green strings
  - executor state handling advanced rotation even after weak/error outcomes
- Replaced `scripts/heartbeat-executor.js` with a safer version:
  - validates and normalizes state
  - writes state atomically
  - adds `lastAttempts` and `lastResults`
  - only advances rotation on verified `ok`
  - checks real local artifacts for `cron`, `sync`, `qmd`, and `health`
  - supports `HEARTBEAT_FORCE_CHECK=<name>` for isolated verification without mutating state
- Verified after patch:
  - `health` -> `Health OK: analysis-2026-07-17.md updated 3h ago; errors logged: 20.`
  - `cron` -> `Cron OK: migrated cron config present; archived run history only.`
  - `qmd` -> `qmd OK: index present (7 MB), updated 147h ago.`

## Lessons

- Heartbeat code should not emit success without touching a real artifact.
- Archived/migrated infrastructure needs different handling than live-runtime checks, otherwise the heartbeat becomes noisy instead of useful.
- A tiny forced-check hook is worth keeping because it makes validation cheap and avoids state churn during debugging.

## Next-run action plan

- Review whether `qmd` staleness should alert at 7 days or a looser threshold.
- Improve `health` to distinguish known-noise errors from fresh regressions instead of only reporting report recency.
- Consider a small open/closed heartbeat issue ledger so repeated reviews can track progress instead of rediscovering the same gaps.

## 2026-07-17 16:04:00 UTC - Heartbeat

- Check: `HEARTBEAT.md` auto-improvement + idle executor rotation
- Executor run: `node /home/claw/.openclaw/workspace/scripts/heartbeat-executor.js`
- Result: routine status `Sync OK: last sync was 23h ago.`
- Outward message: suppressed by guardrail; no executor-raised user-facing alert

### Recent suboptimal outcomes reviewed

- Prior heartbeat reviews kept rediscovering the same executor/infra issues without a closure mechanism, especially across [2026-07-12](2026-07-12.md), [2026-07-14](2026-07-14.md), and [2026-07-16](2026-07-16.md).
- The latest nightly report at `memory/logs/nightly-report-2026-07-16.md` shows 21 failed/issues jobs dominated by rate limits and timeouts, but `checkHealth()` currently treats any fresh report as `ok`.
- Round-robin rotation still risks spending checks on the next slot instead of the most overdue or most suspicious check.

### Concrete improvements

- Teach `checkHealth()` to parse failure volume and repeated signatures from the latest nightly report, then escalate to `alert` when thresholds are exceeded.
- Switch heartbeat rotation from pure round-robin to due-based scheduling with minimum spacing between repeated checks.
- Add a tiny open/closed heartbeat issue ledger so repeated reviews can record closure instead of re-proposing the same fix.

### Parallelization note

- Ran two native Codex explorer subagents in parallel:
  - recent heartbeat-memory issue review
  - executor weak-spot review
- Kept executor execution local because its result determined whether a visible heartbeat alert was warranted.

### Action plan for next run

- Patch `scripts/heartbeat-executor.js` so `health` can alert on high failure counts and repeated rate-limit/timeout patterns.
- Decide the due-based rotation rule before changing state handling again.
- If the next nightly report still shows clustered failures, surface that as a real health alert instead of a silent healthy check.
